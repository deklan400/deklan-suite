# Deklan Suite (starter)

Setup:
1. Clone repo to server (root)
2. Place starter files into `/root/deklan/`:
   - swarm.pem
   - userapikey.json
   - userData.json

3. Put `.env` in `/root/deklan-suite`:

